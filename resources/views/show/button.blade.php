<div class="btn-group " style="margin-right: 2px">
    <button type="{{ $type }}" onclick="{{ $action }}" class="btn btn-sm btn-@color {{$class}}" title="{{ admin_trans('admin.view') }}">
        {{ $text }}
    </button>
</div>

